<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Fragmento AR personalizado para image tracking -->
    <fragment
        android:id="@+id/arFragment"
        android:name="com.example.rutaculturalar.ar.fragments.CustomImageTrackingArFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Instrucciones en la parte superior -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#AA000000">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Botón volver -->
            <Button
                android:id="@+id/btnBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="← Volver"
                android:backgroundTint="#FF5722"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:padding="8dp" />

            <!-- Espaciador -->
            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

        </LinearLayout>

        <!-- Instrucciones mejoradas -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📱 Enfoca una imagen del Campo de Pelota Maya"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginTop="8dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="El modelo 3D aparecerá automáticamente sobre la imagen"
            android:textColor="#CCFFFFFF"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginTop="4dp" />

        <!-- Indicador de estado del tracking -->
        <TextView
            android:id="@+id/trackingStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🔍 Buscando imagen..."
            android:textColor="#FFEB3B"
            android:textSize="11sp"
            android:gravity="center"
            android:layout_marginTop="8dp" />

    </LinearLayout>

</FrameLayout>